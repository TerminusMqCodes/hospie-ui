image: node:20                    # Node.js 20 LTS for Vue.js/Quasar development

clone:
  depth: full                    # SonarQube Cloud scanner needs the full history to assign issues properly

definitions:
  caches:
    sonar: ~/.sonar/cache        # Caching SonarQube Cloud artifacts will speed up your build
    npm: ~/.npm                  # Cache npm dependencies
    
  steps:
    - step: &build-test-sonarcloud
        name: Build, test and analyze on SonarQube Cloud
        runs-on:
          - 'self.hosted'        # Lokális runner használata
          - 'linux'              # Custom label (opcionális)
        caches:
          - node                 # Cache Node.js modules
          - npm                  # Cache npm cache
          - sonar               # Cache SonarQube artifacts
        script:
          # Install dependencies
          - npm ci
          
          # Install Quasar CLI globally
          - npm install -g @quasar/cli
          
          # Lint the code
          - npm run lint
          
          # Format check
          - npm run format
          
          # Run tests
          - npm run test
          
          # Build the project with full memory (local runner advantage)
          - NODE_OPTIONS="--max-old-space-size=8192" npm run build
          
          # Generate test coverage
          - npm run test:coverage
          
          # Run SonarQube analysis with full memory
          - pipe: sonarsource/sonarcloud-scan:4.0.0
            variables:
              SONAR_TOKEN: $SONAR_TOKEN
              SONAR_SCANNER_OPTS: "-Xmx4096m -XX:+UseG1GC"
              EXTRA_ARGS: >
                -Dsonar.projectKey=hospie-ui
                -Dsonar.organization=$SONAR_ORGANIZATION
                -Dsonar.sources=src
                -Dsonar.tests=src
                -Dsonar.test.inclusions=**/*.spec.js,**/*.test.js,**/*.spec.ts,**/*.test.ts
                -Dsonar.exclusions=**/node_modules/**,**/dist/**,**/dist-pwa/**,**/dist-electron/**,**/*.min.js,**/coverage/**,**/.quasar/**
                -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info
                -Dsonar.typescript.lcov.reportPaths=coverage/lcov.info
                -Dsonar.scm.disabled=false
                -Dsonar.javascript.file.suffixes=.js,.jsx
                -Dsonar.typescript.file.suffixes=.ts,.tsx,.vue
              
    - step: &check-quality-gate-sonarcloud
        name: Check the Quality Gate on SonarQube Cloud
        runs-on:
          - 'self.hosted'        # Lokális runner használata
        script:
          - pipe: sonarsource/sonarcloud-quality-gate:0.1.6
            variables:
              SONAR_TOKEN: $SONAR_TOKEN
            
    - step: &build-production
        name: Build for Production
        runs-on:
          - 'self.hosted'        # Lokális runner használata
        caches:
          - node
          - npm
        script:
          # Install dependencies
          - npm ci
          
          # Install Quasar CLI globally
          - npm install -g @quasar/cli
          
          # Build for production with full memory (local runner)
          - NODE_OPTIONS="--max-old-space-size=8192" npm run build
          
          # Build PWA version with full memory (local runner)
          - NODE_OPTIONS="--max-old-space-size=8192" npm run build:pwa
          
          # Build Electron version with full memory (local runner)
          - NODE_OPTIONS="--max-old-space-size=8192" npm run build:electron
          
        artifacts:
          - dist/**
          - dist-pwa/**
          - dist-electron/**
          
    - step: &security-scan
        name: Security Vulnerability Scan
        runs-on:
          - 'self.hosted'        # Lokális runner használata
        script:
          # Install dependencies
          - npm ci
          
          # Run comprehensive security audit (local runner can handle it)
          - npm audit --audit-level=low
          
          # Check for outdated packages
          - npm outdated
          
          # Additional security checks
          - npx audit-ci --config audit-ci.json || echo "Security audit completed"

    - step: &performance-analysis
        name: Performance Analysis
        runs-on:
          - 'self.hosted'        # Lokális runner használata
        script:
          # Install dependencies
          - npm ci
          
          # Build with analysis
          - NODE_OPTIONS="--max-old-space-size=8192" npm run build
          
          # Bundle size analysis
          - npx quasar build --analyze || echo "Bundle analysis completed"
          
          # Check bundle sizes
          - du -sh dist/
          - find dist/ -name "*.js" -exec ls -lh {} \; | head -10

pipelines:                       # Optimized for local runner - no memory limits!
  branches:
    master:
      - step: *build-test-sonarcloud
      - step: *check-quality-gate-sonarcloud
      - step: *build-production
      - step: *security-scan
      - step: *performance-analysis
      
    main:
      - step: *build-test-sonarcloud
      - step: *check-quality-gate-sonarcloud
      - step: *build-production
      - step: *security-scan
      
    develop:
      - step: *build-test-sonarcloud
      - step: *check-quality-gate-sonarcloud
      
  pull-requests:
    '**':
      - step: *build-test-sonarcloud
      - step: *check-quality-gate-sonarcloud
      
  tags:
    'v*':
      - step: *build-test-sonarcloud
      - step: *check-quality-gate-sonarcloud
      - step: *build-production
      - step: *security-scan
      - step: *performance-analysis
      
  custom:
    # Full comprehensive analysis
    full-analysis:
      - step: *build-test-sonarcloud
      - step: *check-quality-gate-sonarcloud
      - step: *build-production
      - step: *security-scan
      - step: *performance-analysis
      
    # Quick development build
    quick-build:
      - step:
          name: Quick Build
          runs-on:
            - 'self.hosted'      # Lokális runner használata
          script:
            - npm ci
            - npm install -g @quasar/cli
            - npm run lint
            - NODE_OPTIONS="--max-old-space-size=8192" npm run build
            
    # Manual deployment pipeline
    deploy-staging:
      - step: *build-test-sonarcloud
      - step: *check-quality-gate-sonarcloud
      - step: *build-production
      - step:
          name: Deploy to Staging
          runs-on:
            - 'self.hosted'      # Lokális runner használata
          deployment: staging
          script:
            - echo "Deploy to staging server"
            # Add your staging deployment commands here
            
    deploy-production:
      - step: *build-test-sonarcloud
      - step: *check-quality-gate-sonarcloud
      - step: *build-production
      - step:
          name: Deploy to Production
          runs-on:
            - 'self.hosted'      # Lokális runner használata
          deployment: production
          trigger: manual
          script:
            - echo "Deploy to production server"
            # Add your production deployment commands here